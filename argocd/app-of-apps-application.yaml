apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-of-app-aps
  namespace: argocd
spec:
  project: default
  
  source:
    repoURL: https://github.com/RafaelJCamara/GitOps.Argo
    targetRevision: HEAD
    path: argocd

  destination:
    server: https://kubernetes.default.svc
    namespace: default

  syncPolicy:
    automated:
      prune: true
      selfHeal: true